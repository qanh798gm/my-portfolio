import { colors } from '../utils/theme'

export function PortfolioPage() {
  return (
    <div
      style={{
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        height: '100%',
        color: colors.textMuted,
        fontSize: 14,
        backgroundColor: colors.bgPrimary,
      }}
    >
      Portfolio Page — Coming in Batch 4
    </div>
  )
}
